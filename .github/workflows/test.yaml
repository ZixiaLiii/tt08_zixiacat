name: "Test"

on:
  pull_request:
  push:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Tiny Tapeout test
        uses: TinyTapeout/tt-support-tools/actions/test@tt08
